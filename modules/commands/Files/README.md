# My Commands files

Ten katalog jest lokalną, testowalną kopią źródeł `MeshCentral-MyCommands`.

Wypełnij lub odśwież go poleceniem:

```powershell
npm run sync:files
```

Skrypt kopiuje pełne źródła z `legacy/commands`, z wyłączeniem `.git`, `node_modules`, danych runtime i logów.
